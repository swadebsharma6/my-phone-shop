import PropTypes from 'prop-types';

const FavoritePhn = () => {
    
    return (
        <div>
       <h4>Favorites phones comes here</h4>
        </div>
    );
};

FavoritePhn.propTypes = {
    phone: PropTypes.object,
    
}

export default FavoritePhn;